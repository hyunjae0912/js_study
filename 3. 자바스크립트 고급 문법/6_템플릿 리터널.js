const name = '또치';

// `` 백틱
// 변수는 ${}로 감싸서 사용
console.log(`안녕하세요. ${name}입니다.`);

const firstName = '길동';
const lastName = '고';

// 문자열 안에 변수와 계산식 삽입하기
console.log(`${lastName} ${firstName}`); // 길동 고

const a = 5;
const b = 10;

console.log(`${a} + ${b} = ${a + b}`); // 5 + 10 = 15
